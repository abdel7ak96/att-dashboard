import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#670DD6',
    },
    secondary: {
      main: '#00B8D9',
      light: '#D7DBE1',
    },
    info: {
      main: '#424B54',
    },
  },
});

export default theme;
